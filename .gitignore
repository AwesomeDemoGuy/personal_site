# Rust build output
/target

# SQLite database files (runtime data — should never be committed)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Environment / secrets
.env
.env.*
!.env.example

# Node (transient; only used to vendor pretext at build time)
node_modules/

# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/
