# Static assets

Files here are copied to the site root and served at `/assets/...`.

`me.jpg` is the profile photo rendered by the draggable photo component
(`src/app.rs` -> `DraggablePhoto src="/assets/me.jpg"`). Replace it with your
own square image; it is displayed as a 150px circle.
